#!/bin/sh

for file in one two
 do
  if one == "d"
   then
    for count in 1 .. $two
     do
      mkdir dir"$one"
    done
  elif one == "jpg" || "pdf" || "txt"
   then
    for count in 1 .. $two
     do
      mkdir $one$two
    done
  fi
done

